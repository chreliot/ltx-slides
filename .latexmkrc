# latexmkrc for reading quizzes

# Use LuaLaTeX
$pdf_mode = 4;

# Display processing time
$show_time = 1;

# Run Biber as needed (disable with 0)
$bibtex_use = 0;

# Generate a file list of inputs and outputs (.fls)
$recorder = 1;